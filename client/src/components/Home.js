import React from 'react';
import Canvas from './Canvas';
import Toolbar from './Toolbar';

const Home = () => {
  return (
    <div>
      <Toolbar />
      <Canvas />
    </div>
  );
};

export default Home;
